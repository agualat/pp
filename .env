# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# API
SECRET_KEY=change_me_in_prod
WORKERS=2
PORT=8000

# Server URL (URL pública del servidor central para sincronización)
# Los clientes usarán esta URL para enviar cambios de contraseña
# Cambiar a la IP/dominio real del servidor en producción
SERVER_URL=http://172.21.230.23:8000

# Frontend
NEXT_PUBLIC_API_URL=http://api:8000

# Composed DATABASE_URL for API and Celery
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:${POSTGRES_PORT}/${POSTGRES_DB}
